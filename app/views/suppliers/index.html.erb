<%= form_tag suppliers_path, :method => :get, :id => "suppliers_search" do %> 
  <p>
    <%= text_field_tag :search, params[:search], placeholder: "Search by supplier name..." %>
  </p>
  <div id="suppliers"><%= render 'suppliers' %></div>
<% end %>
<%= link_to 'Create new supplier', new_supplier_path, :class => 'btn btn-primary' %>