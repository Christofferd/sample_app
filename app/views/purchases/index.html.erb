<%= form_tag purchases_path, :method => :get, :id => "purchases_search" do %>
  <p>
    <%= text_field_tag :search, params[:search], placeholder: "Search by ID, supplier ID, supplier name & status..." %>
  </p>
  <div id="purchases"><%= render 'purchases' %></div>
<% end %>